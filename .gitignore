# Build output
build/

# JUCE is pulled in via a shallow clone; don't commit its source.
# (Re-clone with: git clone --depth 1 --branch 8.0.13 https://github.com/juce-framework/JUCE.git JUCE)
JUCE/

# macOS
.DS_Store
